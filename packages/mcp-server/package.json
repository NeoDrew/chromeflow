{
  "name": "@chromeflow/mcp-server-source",
  "version": "0.9.2",
  "private": true,
  "description": "Source for the chromeflow MCP server. Bundled into packages/plugin/server/ by the plugin's build script. Not published.",
  "type": "module",
  "scripts": {
    "build": "node ../plugin/scripts/build-server.sh 2>/dev/null || ../plugin/scripts/build-server.sh",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ws": "^8.18.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0"
  }
}
